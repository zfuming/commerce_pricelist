{#
/**
 * @file price_list.html.twig
 * Default theme implementation to present Price list data.
 *
 * This template is used when viewing Price list pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_price_list()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('price_list') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
